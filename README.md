## 🛠️ Run Fake API (Simulated Requests)

To simulate real API behavior with a network delay, run the following command in a separate terminal:

```bash
json-server --watch db.json --delay 2000 --port 3001
```
